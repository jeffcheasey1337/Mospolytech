#include<iostream>
#include<cmath>
using namespace std;

double foo(double x)
{
double c;
c = 1.5 - sin(x - 0.6);
return c;
}

int main()
{

setlocale(LC_ALL, "ru");
cout « "Метод простых итераций" « endl;

double a = 0.0, b = 1.0, x = b, x1 = 0, i = 0;
double e = 0.0001;

do
{
x1 = foo(x);

x = foo(x1);

i++;
cout « "Итерация:" « i « endl;

cout « "x = " « x « endl;
} while (foo(x - e) * foo(x + e) > 0 /*abs(x1 - x) > e*/);

cout « "\nРешение уравнения: " « x « endl;
}
